# Copy this file to .env and adjust values. Never commit the real .env.

# Microsoft SQL Server (development container). The password is used both by
# the SQL Server instance and to build the backend connection string; avoid
# characters that require URL encoding.
MSSQL_SA_PASSWORD=YourStrong!Passw0rd
MSSQL_PORT=1433

# Backend
BACKEND_PORT=8000
